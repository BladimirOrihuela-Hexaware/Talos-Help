import { Text } from "@common/components";
import ScreenWrapper from "@services/navigation/components/ScreenWrapper";
import React from "react";

const CloudsScreen = () => {
    return (
        <ScreenWrapper>
            <Text>Hello from /Clouds</Text>
        </ScreenWrapper>
    );
};

export default CloudsScreen;
